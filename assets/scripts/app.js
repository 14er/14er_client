document.addEventListener("DOMContentLoaded", function(event) {
  const USER_URL = BASE_URL + `/api/v1/users/`;
  const hrefLocation = window.location.href;
  const queryStringValue = parseQueryString(hrefLocation);
});
